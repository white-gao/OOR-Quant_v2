# PTQ4RecModel

本项目旨在针对当前的生成式推荐模型设计训练后量化（Post-Training Quantization，PTQ）算法，使模型能够在低位数值格式下进行高效推理，在尽可能保留推荐能力的同时，降低推理时延和显存开销，以满足实际部署需求。

具体而言，本文所讨论的“生成式推荐模型”，特指使用 Semantic ID 表征 item，并基于预训练语言模型进行增训或微调，从而获得推荐能力的模型，代表性工作包括 OneRec 系列，PLUM，RecGPT等。

### Post-Training Quantization

#### Quantization

“模型量化”一般是指将模型的部分参数或中间张量从高精度格式（如 BF16、FP16）映射到低精度格式（8 位及以下，如 INT4、INT8、FP8、MXFP4/6/8），同时将模型的任务性能损失控制在可接受范围内。量化后的数值只能落在低精度格式所能表示的有限数值集合中，因此，该过程不可避免地会引入量化误差。

##### 量化

以常见的均匀仿射定点量化为例，设待量化数值为 $x$，目标整数格式的表示范围为 $[q_{\min},q_{\max}]$，则量化（quantization）和反量化（dequantization）过程可以表示为：

$$
q = \operatorname{clip}\left(\operatorname{round}\left(\frac{x}{s}\right)+z,\ q_{\min},\ q_{\max}\right),
$$

$$
\hat{x}=s(q-z),
$$

其中，$q$ 是量化后的整数，$\hat{x}$ 是反量化后的近似值，$s$ 是缩放因子（scale），$z$ 是零点（zero-point）。在最基础的 min-max 非对称量化中，$s$ 和 $z$ 可以由待量化数据的范围 $[x_{\min},x_{\max}]$ 计算得到：

$$
s=\frac{x_{\max}-x_{\min}}{q_{\max}-q_{\min}},
$$

$$
z=\operatorname{clip}\left(\operatorname{round}\left(q_{\min}-\frac{x_{\min}}{s}\right),\ q_{\min},\ q_{\max}\right).
$$

更为简单的对称量化也较为常用。对称量化可以视为上述仿射量化的特殊情况，通常令 $z=0$，并使用以零为中心的量化范围。FP 量化可以采用基于绝对值最大值（absmax）的对称缩放。设一个量化组内的数值集合为 $x$，目标 FP 格式的最大有限正数为 $Q_{\max}$，则缩放因子为：

$$
s=\frac{\max_i |x_i|}{Q_{\max}},
$$

$$
\hat{x}=s\cdot \operatorname{Cast}_{\mathcal{F}}\left(\operatorname{clip}\left(\frac{x}{s},-Q_{\max},Q_{\max}\right)\right).
$$

其中，$\mathcal{F}$ 表示目标 FP 格式所能表示的数值集合，$Q_{\max}$ 表示该格式能够表示的最大有限正数。

##### 量化误差

量化误差可以表示为 $\varepsilon_q=\hat{x}-x$，主要来自数值向有限量化格点映射时产生的舍入误差，以及数值超出可表示范围时产生的截断误差。位宽越低，可用量化格点越少；当一个量化组的动态范围较大或包含异常值时，缩放因子通常也会增大，导致主体数值能够利用的有效量化分辨率下降。因此，量化范围、量化粒度和异常值处理方式都会直接影响最终的量化误差。

##### 量化粒度与缩放方式

缩放因子可以由不同范围内的数值共享，由此形成不同的量化粒度。常见方式包括 per-tensor、per-channel、per-group，以及激活量化中常用的 per-token。通常，粒度越细，每个量化组内的动态范围越容易控制，量化误差也可能越小，但同时会引入更多缩放参数，并增加存储、计算和算子实现的复杂度，所以量化粒度就需要根据任务灵活选择。一般情况下，权重量化使用per-channel/per-group-128粒度，激活值常用per-token粒度。在对比不同PTQ算法时，需要统一对比的量化粒度从而保证公平。

根据缩放因子的确定时机，量化还可以分为静态量化和动态量化。静态量化在推理前根据权重或校准数据确定量化参数；动态量化则在推理过程中根据当前输入实时计算量化参数。模型权重通常采用静态量化，而激活值既可以采用静态量化，也可以采用动态量化。

根据量化所处的阶段，模型量化可以分为“量化感知训练（Quantization-Aware Training，QAT）”和“训练后量化（Post-Training Quantization，PTQ）”：

- QAT：在模型训练期间引入量化，并将量化损失纳入模型优化目标，使模型能够在训练过程中适应自身的量化误差。QAT 是减小量化损失的有效方法，但训练开销通常较大。
- PTQ：在模型训练结束后对模型进行量化，使用少量校准数据模拟真实场景下的推理输入，并通过对齐量化模型与全精度模型在校准集上的输出，尽可能保留模型的基本能力。与 QAT 相比，PTQ 的训练开销更小。

模型量化的对象主要包括模型权重（weight）、激活值（activation）和 KV Cache 等。一般而言，根据量化对象的不同，量化算法可以分为 weight-only 量化和 weight-activation 量化。

- weight-only：仅对 weight 进行量化，activation 保持高精度。该方法能够有效减小推理期间的显存占用并提升数据搬运效率；在推理计算时，weight 会被反量化至高精度，再与 activation 进行计算。
- weight-activation：weight 和 activation 均在推理阶段参与量化。其中，weight 通常经过离线量化后保存，activation 则在推理过程中动态量化。一般情况下，二者采用同一种低位数值类型，从而支持低位矩阵运算（如 INT8、FP8），加速推理计算。

##### 量化的存储与计算收益

量化带来的部署收益主要来自存储、内存带宽和计算三个方面。忽略缩放因子等元数据时，相比每个元素占用 16 bit 的 BF16，8-bit 和 4-bit 权重的理论存储量分别约为其二分之一和四分之一；更小的数据体积也可以减少权重读取和数据搬运开销。当硬件和计算内核原生支持相应低位格式时，weight-activation 量化还可以利用低精度矩阵计算单元提升计算吞吐。需要注意的是，低位数值格式并不会自动转化为实际加速：最终收益仍取决于量化存储格式、算子实现和硬件支持，而伪量化实验只能评估数值与任务性能损失，不能直接证明真实部署中的显存和速度收益。

在实验配置中，通常使用 $\mathrm{W}b_w\mathrm{A}b_a$ 表示权重和激活的量化位宽。例如，W4A8 表示权重采用 4-bit 格式、激活采用 8-bit 格式。目前在PTQ领域，8bit量化已经默认不再考虑，大多数论文都不会展示W8A8的效果（因为损失是在太小，导致所有基线都在benchmark表现很好），目前量化位数的主战场在4bit量化及以下。但是在应用中，8bit量化是一个非常常用的选择，目前市面上有许多量化工具，支持针对主流模型（LLaMa，Qwen...）进行一键量化（可以自己选择使用的主流量化算法，平滑算法，量化位数，量化粒度等），一键量化的模型也可以被vllm等推理框架支持。如果想要快速地得到一个可部署的量化模型，直接使用量化工具会很方便。
#### PTQ Basics

PTQ 算法的主要思路是“对齐全精度模型”，常用方法是进行“逐层对齐优化”。目前，较为主流的对齐目标是最小化对应位置输出之间的均方误差（Mean Squared Error，MSE）。

PTQ 算法的设计核心是减小量化精度损失，而量化损失与量化组内参数的数值分布密切相关。如果同一量化组内待量化参数的数值更加接近，该组参数的量化损失通常也会更小。因此，大多数算法会设计相应的等价变换，对原本数值差异较大的模型参数进行“平滑”，进而减小量化损失。

### 为什么要做推荐模型的训练后量化算法？

首要目的是加速推理。目前，全精度模型的推理速度难以满足实际部署的时延要求，因此，我们考虑通过训练后量化技术降低模型的数值精度，提高推理速度，同时尽可能保留模型的推荐能力。

为了探究 PTQ 算法在推荐模型中的应用，本文采用目前仅有的开源生成式推荐模型 OneRec-1.7B/8B，并使用对应的 Rec-IF Benchmark 进行评测，主要关注其中的四个 next item prediction 任务（ad、product、video、xx），暂不考虑模型在其他任务上的能力。

本文采用伪量化方式进行实验，即在高精度计算环境中，将推理过程中的数值映射到低精度数值格式所能表示的离散格点，但仍使用高精度数据类型完成实际计算。其主要目的是模拟量化模型的推理数值，从而衡量不同量化算法在任务性能保持方面的优劣；实际部署场景下的推理时延暂不在本文的研究范围内。

#### 通用量化方法在推荐模型上的局限

当前，使用通用量化方法对推荐模型进行量化时，主要存在以下问题：**通用大模型在 8-bit 量化场景下已经能够做到几乎无损（>99.5%）；而在 Rec-IF 的多个推荐任务中，8-bit 量化通常会带来 3%～5% 的性能损失，尚未达到无损量化水平。**

经过分析，可能有以下几个原因：

1. 模型本身在推荐任务上的训练还不够充分，其输出分布极为平坦；相比之下，大模型在通用任务上的输出分布更加尖锐，即使面对较难的推理任务，也能达到 xx% 以上的首 token 概率。
2. 模型的解码方式可能会进一步放大平坦输出分布所带来的误差，例如 beam search……
3. 推荐任务本身难度较高，且评价指标较为稀疏。

#### 可以优化的方向

1. 输出端导向的思路：从输出端入手，基于“**只要输出分布与全精度模型足够相似，就能达到接近无损的任务性能**”这一想法，对齐模型在有限输出空间内的分布。其难点在于，高概率 token 的占比极小，输出会很快坍塌至平坦区域，因此很难维持 token 之间的相对概率顺序，也难以识别并保护平坦区域内的关键 token。
2. PTQ 的本质优化思路——**最小化量化损失**：在现有通用训练后量化算法的基础上，根据推荐任务或模型的相关特性，对量化粒度或分组方式进行进一步且合理的细分，从而减小量化过程中的量化损失，并最终降低量化对模型任务性能的影响。
